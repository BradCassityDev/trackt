import React from 'react';
// import {  } from '../utils/queries';
import Auth from '../utils/auth';

const Home = () => {

  return (
    <main>
      Main content
    </main>
  );
};

export default Home;