import { ADD_FRIEND, REMOVE_FRIEND, ACCEPT_FRIEND } from './mutations';
import { useMutation } from '@apollo/react-hooks';

// Add Friend
const addFriend = (friendId, userId) => {
    
};

// Remove Friend
const removeFriend = (friendId, userId) => {

};

// Accept Friend Request
const acceptFriendRequest = (requestId, friendId, userId) => {

};


export default {
    addFriend, 
    removeFriend,
    acceptFriendRequest
};